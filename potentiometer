void setup() {
  
Serial.begin(9600);

//declare pin D8 as output
pinMode(8,OUTPUT);


void loop() {

//for reading analog value of potentiometer through pin A0
float pot_value=analogRead(A0);

//condition for glowing LED
if (pot_value>2.5)
digitalWrite(8,HIGH);
else
digitalWrite(8,LOW);
}
